int rose(int x, int y) {
    for (int i = 0; i < 10; i = i + 1) {
        x = x *2; y = y*3;
    }

    return x % -y;
}

void mary(bool z)
    if (z == true) {
        return !z;
    } else 
        return z && (0 ^ z);
