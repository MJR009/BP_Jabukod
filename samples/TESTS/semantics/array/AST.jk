int main() {
    int y;
    int x[5];
    //int x[2.3];
    //int x[1 + 1];

    float base = 1.5;

    //int def[3] = 1 + {base*1, base*2, base*3};
    //int def = 1 + {base*1, base*2, base*3};
    //bool def = {base*1, base*2, base*3};
    //int def[3] = {base*1, base*2, base*3, base*4};
    int def[3] = {base*1, base*2, base*3};

    //y = def;
    y = def[1];

    //def[0] = 1 + y[0];
    def[base + def[1]] = def[0];
    //def = base*4;
    def[2] = base*4;
}

void rose() {
    return;
}
