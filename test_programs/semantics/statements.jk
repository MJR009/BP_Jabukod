/*int x = 0;

int main() {
    if (x <= 10) x = x + 1; else x = true;

    while (x != 10) {
        x = x + 1;
        int y = x * 2.5;
    }

    for (int x = 0; ; x = x + 1) {
        string y = "Hello\n";
        write y;
    }

    for (;;) write "uh oh\n";

    foreach (int i : x) {
        return 1;
    }
}
*/

int main() {
    main();
    while (true) {
        return 0;
    }

    if (false) {
        exit 0;
    }

    return 0;
    exit 0;
    // continue;
    // break;
    // redo;
    // restart;
    string x;
    write "Hello!\n";

    int y;
    int z;

    z = y = 1;
}
